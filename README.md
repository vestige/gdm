# gdm

Good Morning 
https://vestige.github.io/gdm/

## Env (serverless)

Google Places を使うお得ヒント API は `GOOGLE_MAPS_API_KEY` を使います。

- Vercel の Environment Variables に `GOOGLE_MAPS_API_KEY` を設定
- ローカルで serverless を試す場合も同じ名前で設定
